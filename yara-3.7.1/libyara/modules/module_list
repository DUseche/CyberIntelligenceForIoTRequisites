MODULE(tests)
MODULE(pe)
MODULE(elf)
MODULE(math)
MODULE(time)

#ifdef DOTNET_MODULE
MODULE(dotnet)
MODULE(r2)
#endif

#ifdef CUCKOO_MODULE
MODULE(cuckoo)
#endif

#ifdef MAGIC_MODULE
MODULE(magic)
#endif

#ifdef HASH_MODULE
MODULE(hash)
#endif
